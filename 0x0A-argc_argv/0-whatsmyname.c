#include "main.h"
#include <stdio.h>

/**
 * main - entery code.
 * @argc: argument count.
 * @argv: argument vector.
 * Description: A code that prints it's name.
 *
 * Return: Always (0) Success.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
