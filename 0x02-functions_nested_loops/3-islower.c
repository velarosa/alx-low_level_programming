#include "main.h"

/**
 * _islower - does my stuff
 * @c: its mine damnit
 * Description: Does my stuff explained.
 *
 * Return: 1 if Success 0 in not
*/

int _islower(int c);
{

        if (c >= 97 && c <= 122)
        {
        return (1);
        }
        else
        {
        return (0);
        }
}
