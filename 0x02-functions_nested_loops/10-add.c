#include "main.h"

/**
 * add - Function that adds two integers together.
 * @a: the first inputed integer.
 * @b: the second inputed integer.
 * Description: two integers gets add in result, and result is returnd.
 *
 * Return: Always (result) Success.
*/

int add(int a, int b)
{
	int a = 0;
	int b = 0;
	int result = a + b;

	return (result);
}

