#include "main.h"

/**
 * mul - A function that multiplies tow integers and reeturns the result.
 * @a: first integer to be multiplied.
 * @b: second integer to be multiplied.
 * Description: multiply a and b and printt resulte.
 *
 * Return: Always (result) Success.
*/

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
